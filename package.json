{
  "name": "@jg/react-template-builder",
  "version": "0.0.1",
  "description": "",
  "repository": "jakubgereg/react-template-builder",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/**.*"
  ],
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write '**/*.{ts,tsx,js,html}'",
    "format:check": "prettier --check '**/*.{ts,tsx,js,html}'",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "check": "npm run format:check && npm run typecheck && npm run test",
    "preversion": "npm run check",
    "postversion": "git push && git push --tags && npm publish",
    "prepublishOnly": "npm run build",
    "test": "jest"
  },
  "keywords": [
    "json",
    "react",
    "template",
    "builder",
    "engine",
    "dynamic",
    "web"
  ],
  "author": {
    "name": "Jakub Gereg"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "29.5.5",
    "@types/react": "18.2.61",
    "@types/react-test-renderer": "18.0.7",
    "jest": "29.7.0",
    "prettier": "3.2.4",
    "react-test-renderer": "18.2.0",
    "ts-node": "10.9.2"
  },
  "dependencies": {
    "ts-jest": "29.1.2",
    "typescript": "5.3.3"
  },
  "peerDependencies": {
    "react": "18.2.0"
  }
}
